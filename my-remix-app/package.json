{
  "name": "my-remix-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "PORT=${PORT:-6969} tsx watch server.ts",
    "start": "PORT=${PORT:-6969} tsx server.ts",
    "test": "tsx --test",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=24.3.0"
  },
  "dependencies": {
    "remix": "^3.0.0-beta.0",
    "tsx": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  }
}
